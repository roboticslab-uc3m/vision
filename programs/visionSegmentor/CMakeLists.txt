# Generated by "yarp cmake" & Juan G Victores 2012 @ UC3M

option(ENABLE_visionSegmentor "Enable/disable visionSegmentor program" ON)

IF (ENABLE_visionSegmentor)

set(KEYWORD "visionSegmentor")

find_package(OpenCV REQUIRED)
find_package(YARP REQUIRED)

include_directories(${YARP_INCLUDE_DIRS}
                    ${OpenCV_INCLUDE_DIRS})

add_executable(visionSegmentor main.cpp
                               SegmentorThread.cpp
                               SegmentorThread.hpp
                               VisionSegmentor.cpp
                               VisionSegmentor.hpp)

target_link_libraries(visionSegmentor YARP::YARP_OS
                                           YARP::YARP_init
                                           YARP::YARP_dev
                                           YARP::YARP_sig
                                           ${OpenCV_LIBS}
                                           TravisLib)

install(TARGETS visionSegmentor
        DESTINATION ${CMAKE_INSTALL_BINDIR})

ENDIF (ENABLE_visionSegmentor)

